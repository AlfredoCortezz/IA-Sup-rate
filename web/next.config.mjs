/** @type {import('next').NextConfig} */
const nextConfig = {
  // Con esto ya puedes desplegar en Vercel sin tocar nada más.
  // Si más adelante conectas un backend real (NestJS/FastAPI), aquí podrías
  // agregar reglas de reescritura (rewrites) o cabeceras CORS.
};

export default nextConfig;